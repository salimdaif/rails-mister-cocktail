<h1>Doses#new</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, placeholder: "Muito"%>
  <%= f.association :ingredient %>
  <%= f.submit "Add a Dose", class: "btn btn-primary" %>
<% end %>
